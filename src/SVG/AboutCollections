Коллекции в жаве могут поддерживать или не поддерживать добавление, удаление итд

ArrayList поддерживает все

myRedCircle.getTags() - это List(Tag)
и если это ArrayList, то мы можем добавить новый тэг и это странно, тк мы же уже задали и НИКто не должен менять

что делают в атком случае? есть разные подходы
1) метод getTags каждый раз возвращает новую коллекцию -> если что-то изменили, то новая коллекция вернется
неэффективно

2) хочется хранить одну коллекцию для каждых всех красных кругов, тк все одинаковые
нужны коллекции, которые невозможно изменять
и есть такие коллекции
    2.1) Collections.unmodifiableList(...) - оборачивает коллекцию в себя так, что менять больше не выйдет, даст ошибку

        такая штука называется proxy, он делает неизменяемое, а методы делигирует в исходную коллекцию

    2.2) Сразу создаем неизменяемое, есть много методов

        Arrays.asList(...,...,...,)

        в Java11 List.of()

        Collections.singletonList(), но к шаблону в задаче относится только тем, что слово то же самое, одноэлементыый список
        есть функции singletonSet(); .emptyList();

