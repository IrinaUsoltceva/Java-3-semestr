# Рисование SVG

    в СВГ мы рисуем не пикселями, а фигурами - треугольнички, квадратики
    повернуть и тд
    по сути, это векторное рисование
    формат свг основан на векторной графике
    мы можем создавать из текстового файла, просто говоря что и где рисовать
    изображение состоит из набора тегов, похоже на html
    обязаны закрывать все теги в xml
    
    заголовок виден из примера - s
    vg, некое заклинание с xmlns, показывающее, что это свг
    потом ширина-длина, говорим тип, ху, высоту-ширину, стилистика
    на сайте справочник, там есть элементы, а также атрибуты
    
о программе
    
    ```
    пишем программу из большого количества маленьких классов
    которая хаотично рисует свг на чем-то
    
    по сути, у нас будет класс круг, где есть круг красного цвета
    на него ссылаемся, как на круг
    в настройках - фиксированный набор элементов, которые заранее задали и 
    каждый из которых имеет свой класс
    
    пока начинаем с того, что учимся рисовать свг
    свг, имя файла, в который записывается, его размеры
    потом добавляем теги (помним, что теги записываются как в хтмл)
    в примере мы добавляем два прямоугольника на свгшу
    ```
    
что такое один тег?
    
    при создании тега мы создаем один прямоугольник, например, 
    в 200 200, имеющий маленькую заливку
    по сути, класс тег это обертка над ассоциативным массивом?
    set - в ассоциативный массив записывается информация
    а потом записывается в файл?....
    и первое, с чего начинаем - нужно заставить работать
    
    мы создаем файл свг с изображением 
    
что будет потом?
    
    потом мы делаем так, чтобы не делать клоз вручную
    добавляем класс фигуры, ту самую абстракцию


1. Вспомогательные классы для рисования
    1. Класс SVG, содержит открытый PrintStream для печати SVG:
    ```
        Svg svg = new SVG("a.svg", 300, 300);
        svg.addTag(rect1);
        svg.addTag(rect2);
        svg.close();
    ```
    В конце обязательно будет вызвано закрытие, чтобы закрыть PrintStream
    PrintStream - класс, который позволяет удобно печатать файл
    
    2. Класс Tag - это описание одного тега
    
    ```
        Tag rect1 = new Tag("rect");
        rect1.set("x", "200");
        rect1.set("y", "200");
        rect1.set("width", "10");
        rect1.set("height", "20");
        rect1.set("style", "stroke:#ff0000; fill: #0000ff");
    ```
    
3. Когда сделаете классы Tag и SVG, проверьте, что можете создавать картинки с их помощью

4. Класс Shape. Фигура. Изображение будет рисоваться из болоьшого числа фигур. 
Какие именно фигуры будут использооваться для рисования, будет задаваться в настройках.
Этот класс должен иметь компьютер без аргументов. 
И он имеет метод getTags(), который возвращает массив тегов для рисования этой фигуры

5. Подумаем над расположением фигур. Они должны быть случайным образом разбросаны по рисунку
Создадим класс PositionsShape - это фигура плюс ее положение. 
При этом он сам должен являться фигурой (extends Shape)
Он имеет поля Shape shape, int x, int y
Для рисования он добавляет каждому тегу фигуры дополнительный параметр 
`transform` со значением `translate(30, 30), Где 30, 30 - это x, y

6. дальше добавим файл Properties, где указывается 
    * размер картинки
    * количество фигурок, которые надо нарисовать
    * набор фигурок для рисования
    *...
    
7. Понадобится фабрика, чтобы создать фигурки из описания в файле Properties. 

8. Понадобится адаптер, чтобы превращать png картинки в фигуры

9. Понадобятся дектораторы, чтобы рисовать рамки вокург фигурок или как-то иначе их модифицировать

10. ...